#include<stdio.h>
int main()
{
    int num;
    float f;
    char c;
    double d;

    int *pnum;
    float *fp;
    char *cp;
    double *dp;

    printf("size of n is %d bytes \n",size of(int));
    printf("size of n is %f bytes \n",size of(float));
    printf("size of n is %c bytes \n",size of(char));
    printf("size of n is % bytes \n",size of(int));



